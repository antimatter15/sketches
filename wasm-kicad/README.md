# wasm-kicad

Self-contained static copy of the KiCad import and preview webapp.

Run it from a static file server rooted at this directory and open
`index.html`. The generated KiCad runtime is included locally:

- `wasm/kicad_cli.js`
- `wasm/kicad_cli.wasm`
